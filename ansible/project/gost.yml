---
- name: Download latest gost
  hosts: 127.0.0.1
  connection: local
  gather_facts: no
  tasks:
    - setup:
  roles:
    - role: gost_download
      when: update_gost is defined and update_gost

- hosts: "{{ host | default('web') }}"
  become: yes
  gather_facts: no
  tasks:
    - setup:
  roles:
    - role: gost_sync
      when: update_gost is defined and update_gost
    - role: gost_get
      when: update_gost is defined and update_gost
    - gost